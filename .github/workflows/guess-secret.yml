# name: Check secret from comment

# on:
#   issue_comment:
#     types: [created]

# permissions:
#   issues: write
#   pull-requests: write

# jobs:
#   check:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Check comment against secret
#         id: check
#         env:
#           COMMENT: ${{ github.event.comment.body }}
#           SECRET: ${{ secrets.CHECK_SECRET }}
#         run: |
#           if [ "$COMMENT" = "$SECRET" ]; then
#             echo "result=OK" >> $GITHUB_OUTPUT
#           else
#             echo "result=NOT_OK" >> $GITHUB_OUTPUT
#           fi

#       - name: Reply with result
#         uses: actions/github-script@v7
#         with:
#           script: |
#             const result = "${{ steps.check.outputs.result }}";
#             const body = result === "OK" ? "OK" : "Not OK";

#             await github.rest.issues.createComment({
#               issue_number: context.issue.number,
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               body
#             });
